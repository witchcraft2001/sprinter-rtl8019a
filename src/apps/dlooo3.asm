; DLOOO3 -- DLWIN3 plus a two-slot direct TCP out-of-order queue.
; Developer-image experiment only; production clients retain DLWIN3/DIRECT.
	DEFINE DLDIRECT_WIN3
	DEFINE DLDIRECT_OOO
	INCLUDE "dldirect.asm"
