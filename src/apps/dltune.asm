; DLTUNE -- direct single-session TCP window/ACK experiment.
; Developer floppy only; the production DLL and WGET/FTP builds do not
; define any of the tune/OOO branches.
	DEFINE DLDIRECT_TUNE
	DEFINE DLDIRECT_OOO
	INCLUDE "dldirect.asm"
