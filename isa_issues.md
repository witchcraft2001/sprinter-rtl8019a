# Анализ проблем ISA-доступа RTL8019AS на Sprinter DX (v3.06)

Документ для разработчиков. Описывает расследование нестабильной работы
сетевой карты RTL8019AS на шине ISA Спринтера, корневую причину и предложения
по исправлению. Это исключительно файл исходного дерева — он **не** входит ни в
один из `DIST_*`-списков и не поставляется в дистрибутиве.

Дата: 2026-06-10. Целевая платформа: Sprinter DX, плата `spdx_release_v306`
(25.05.2025), прошивка ACEX (`sprinter_bios`).

---

## 1. Краткое резюме (TL;DR)

- Драйвер/стек логически корректны: ранее на этом же железе уже наблюдались
  настоящий DHCP-lease и ответы `ping` (`TTL=64`). Проблема — в **надёжности
  доступа к чипу по шине ISA**, а не в протокольной логике.
- Все «битые» чтения дают именно `0xFF`, потому что буферизованная шина данных
  **подтянута к VCC резисторами 4.3 КОм** (R201–R208). Любое чтение, где карта
  не успела выставить данные, защёлкивается как `0xFF`.
- FPGA даёт ISA-обращениям **фиксированное окно ожидания ~95 нс** и **не
  заводит сигнал `IOCHRDY`** (I/O CH READY, A10 слота) в логику `/WAIT`. Поэтому
  медленный порт данных remote-DMA не может попросить продлить цикл.
- Регистры (0x00–0x0F) отвечают быстро и читаются; **порт данных DMA (0x10)**
  не успевает за ~95 нс → читается подтяжка `0xFF`. Под нагрузкой обращений к
  порту данных больше → больше сбоев и «зависаний» чипа.
- Форумная версия про «паразитный `/MEMR`» / «кривизну ISA» относится к
  **старым платам**. На v3.06 тракт чтения структурно **чистый** (см. §5).
- Со стороны Z80 это непоправимо в принципе (нужно растягивать сам ISA-цикл).
  Рекомендуемый порядок фикса (по возрастанию инвазивности):
  1. **Прошивка, без правок платы:** увеличить `WT_R` (§7.1). Не нужны ни
     свободный вывод, ни `IOCHRDY`. Первый тест гипотезы. Проверка — `NICRAM`.
  2. **Вне ПЛИС (если перешивать нельзя):** внешний генератор ожидания по
     `IOCHRDY` на GAL/CPLD (§7.2.B2); быстрый костыль — фикс. удлинитель (B1).
     Возможно, т.к. `/WAIT` — открытый сток (§5.5б).
  3. **Внутрь ACEX (`IOCHRDY`→`/WAIT`, §7.2.A):** корректно, но ACEX занят
     полностью (147/147 выводов, §5.5а) — сейчас непрактично.

---

## 2. Наблюдаемые симптомы

- DHCP периодически не получает адрес: иногда `got OFFER`, затем пауза и
  таймаут; иногда вообще нет OFFER.
- `ping`: проходит 0–1 ответ из 4, остальные теряются.
- В диагностических дампах регистров часто `CR=FF ISR=FF BNRY=FF CURR=FF` —
  при том, что `CR` (командный регистр) на живом чипе **никогда** не равен
  `0xFF`. То есть в момент дампа чип вообще не выставляет данные на шину.
- Иногда отдельный кадр читается с телом из сплошных `FF` (диагностика
  `ping` показывает `last type=FFFF`).
- Поведение нестабильно от запуска к запуску; усиливается под фоновым трафиком
  (ARP-скан шлюза, широковещательный UDP от соседних хостов).

Контрапункт: ранее на этом же железе **проходил** `NICRAM` на всех размерах и
был реальный DHCP-lease. Значит это не «стена», а маргинальное качество
доступа, срывающееся под нагрузкой/таймингом.

---

## 3. Методология проверки

1. Схема платы: `board/spdx_release_v306_25.05.2025/docs/sp-dx-sch.pdf`,
   страница 8 «ISA-B BUS» (автор Ivan Mak).
2. Прошивка ПЛИС: `sprinter_bios/src/altera/quartus/acex/SP2_ACEX.TDF`
   (идентична `sprinter_bios/src/altera/acex/k30/SP2_ACEX.TDF`).
3. Сравнение с рабочим продуктом: ESP/Wi-Fi (`sprinter_wifi`), который ходит по
   той же шине ISA через UART TL16C550 и работает стабильно.
4. Внешний источник: тема форума nedoPC t=20283 «Проект Sprinternet» (плата на
   WizNet W5100 + CPLD) — независимое свидетельство о квирках старой ISA.

---

## 4. Что НЕ является причиной на v3.06

### 4.1. «Паразитный `/MEMR`» / «кривизна ISA»

В теме форума автор Sprinternet пишет про «уже доказанную кривизну
спринтеровской исы» и защиту от «мусорных `/MEMR`», решённую **аппаратно** в
CPLD его собственной платы. Это относится к **старым ревизиям**. На v3.06
тракт формирования стробов чистый (см. §5.2), грубого источника ложных стробов
не видно. Поэтому «паразитный `/MEMR`» как причину текущих сбоев **отвергаем**.

### 4.2. Тайминг со стороны Z80 (`RTL_DMA_SETTLE`)

Вставка задержек **между** чтениями порта данных не удлиняет сам ISA-цикл и
длительность строба. Карта выставляет данные только во время строба; если
строб/цикл короткий — лишние Z80-паузы между обращениями не помогают. Это
согласуется с прошлым экспериментом (`RTL_DMA_SETTLE=100` не дал эффекта).

---

## 5. Корневая причина

### 5.1. Шина данных подтянута к VCC → undriven = `0xFF`

Схема, стр. 8: буферизованная шина `BD0..BD7` имеет подтягивающие резисторы
**4.3 КОм к VCC** (R201–R208). Следствие: если ISA-устройство не успело
выставить валидные данные к моменту защёлки, хост читает `0xFF`. Это —
физический источник всех наблюдаемых «FF».

### 5.2. Тракт чтения структурно корректен

Схема, стр. 8:
- Стробы формирует дешифратор **74HC138** (A0=RA14, A1=WR/, A2=RA15;
  разрешения RA16/RA17/CS_CASH) и гейты **74HC32 / 74HC02** →
  раздельные `MEMR1//2/`, `MEMW1//2/`, `IORD1//2/`, `IOWR1//2/`.
- На I/O-стробах стоит **последовательное демпфирование 51 Ом**
  (R188 / R194 / R195 / R209) — корректная мера целостности сигнала.
- Шина данных проходит через буферы **74HC245**.
- Слоты выдают **A1 = I/O CH CHECK** (→ цепь `IOCH`) и
  **A10 = I/O CH READY (IOCHRDY)**.

Замечаний к структуре read-path нет.

### 5.3. Фиксированное окно ожидания ~95 нс, `IOCHRDY` не заведён

Прошивка `SP2_ACEX.TDF`:

- Полный список выводов верхнего уровня (строки 26–72) содержит **только**:
  - вход тактирования `TG42`;
  - входы `/M1 /RF /IO /WR /RD /HALT /MR A[15..0]`;
  - двунаправленные `/WAIT /RESET D[7..0]` и шины видео/DRAM;
  - выходы `CS_ROM CS_CASH RA[17..14]` и периферии.
- **Входа `IOCHRDY/IORDY/RDY` нет вообще.** Значит `I/O CH READY` слота
  (A10) физически некуда подать в логику ожидания.
- `/WAIT` для ROM/ISA — фиксированный счётчик (строки 593–601):

  ```
  WAIT_ROMX = LCELL(CS_ROM & CS_ISA);
  WAIT_ROM  = (WAIT_ROMX or DFF((WT_R[] == 0),CLK42,!WAIT_ROMX,));
  WT_R[].clk = CLK42;
  CASE (WAIT_ROMX,WT_R[]) IS
      WHEN 0       => WT_R[] = 0;
      WHEN B"0001" => WT_R[] = WT_R[] - 1;
      WHEN B"001X" => WT_R[] = WT_R[] - 1;
      WHEN B"01XX" => WT_R[] = WT_R[] - 1;
      WHEN B"1XXX" => WT_R[] = 4;   -- перезагрузка счётчика
  END CASE;
  ```

  `WT_R[2..0]` загружается значением **4** и считает вниз на `CLK42`
  (мастер-такт `TG42`, ~42 МГц → ~23.8 нс на такт) ≈ **95 нс** ожидания.
  `IOCHRDY` в формировании `/WAIT` (строки 905–909) не участвует.

### 5.4. Полный механизм сбоя

1. Хост инициирует чтение ISA (память окна 0xC000…, режим ISA-портов).
2. FPGA держит `/WAIT` фиксированные ~95 нс — и не слушает `IOCHRDY`.
3. **Регистры** RTL8019AS (0x00–0x0F) отвечают достаточно быстро, успевают
   выставить данные за ~95 нс → читаются корректно.
4. **Порт данных remote-DMA** (0x10) медленнее: ему нужна задержка
   FIFO/локального DMA — это ровно тот случай, когда настоящая ISA-карта
   поднимает `IOCHRDY`, прося подождать. Запрос игнорируется.
5. К моменту защёлки шина ещё не driven → хост читает подтяжку = `0xFF`.
6. Под нагрузкой обращений к порту данных больше → больше `FF`, рассинхрон
   авто-инкрементного указателя DMA, и «зависание» чипа (`CR=FF ISR=FF`).

Сравнение с ESP/Wi-Fi: UART TL16C550 — быстрый чип с идемпотентными чтениями
регистров, ему ~95 нс хватает, поэтому продукт работает. RTL8019AS с
авто-инкрементным портом данных DMA к таймингу куда чувствительнее.

### 5.5. Два факта, определяющих способ фикса (проверено 2026-06-10)

**(а) ACEX занят полностью — свободных выводов нет.**
Устройство `EP1K30QC208-3` (ACEX 1K30, 208-выводной PQFP) имеет **147
пользовательских I/O**. В `SP2_ACEX.qsf` занято ровно **147** уникальных
`PIN_…`. То есть **завести `IOCHRDY` (A10) прямо в ACEX некуда** без
высвобождения существующего вывода. «Дырки» в нумерации 1..208 — это
питание/земля/служебные выводы конфигурации, не пользовательские I/O.

**(б) `/WAIT` — открытый сток, и ПЛИС читает линию обратно → внешний wait
безопасен.**
`/WAIT` — вывод **18** ACEX. В прошивке (`SP2_ACEX.TDF`):

```
:909   /WAIT     = TRI(/WAIT_ALL, !/WAIT_ALL);
:905   /WAIT_ALL = (DECODE./WAIT & WAIT_ROM & WAIT_ORIG);
```

- `TRI(...)` означает: ПЛИС **тянет `/WAIT` в 0** (вставить ожидание), либо
  отпускает в **Hi-Z** (линию держит подтяжка к VCC), и **никогда не гонит в 1**.
- `DECODE./WAIT` — ПЛИС **читает физическую линию `/WAIT` обратно** и подмешивает
  её в `/WAIT_ALL`: увидев линию низкой, сама уходит в Hi-Z.

Следствие, важное для фикса: **любой внешний открытый сток может удерживать
`/WAIT` в 0 без конфликта с ПЛИС** — Z80 ждёт, пока линия низкая (он сэмплит
саму линию), а ПЛИС в это время не сопротивляется. Значит обработку `IOCHRDY`
можно сделать **вне FPGA** (см. §7.2, вариант B).

Дополнительно: слот выдаёт ещё `B8 = NOWS/0WS` (цепь `0WS` — «нулевое ожидание»,
противоположный смысл) и `A1 = I/O CH CHECK` (цепь `IOCH`). У `A10 (I/O CH
READY)` именованной цепи нет — он разведён между слотами/подтянут и в логику
ожидания не идёт. ISA-стробы `IORD1//IOWR1//MEMR1//MEMW1/` (с дешифратора
74HC138, стр. 8) и системный такт на плате доступны — это всё, что нужно
внешнему генератору ожидания.

---

## 6. Что уже сделано на стороне драйвера (программные смягчения)

Это **не** лечит шину, но не даёт системе деградировать/зависать; потери
добирают ретрансмиты протокола (ARP/DHCP/ping retry). Изменения 2026-06-09/10
в `src/`:

- `ifup`/`ping`: убран промискуитет, переключены на `RCR_AB` — на шину больше
  не вытягивается весь сегментный трафик (меньше переполнений и долгих
  IRQ-off DMA-чтений).
- Драйвер `rtl8019.asm`: добавлено восстановление после переполнения RX-кольца
  (`RECOVER_OVERFLOW`), вызывается из `RING_HAS_PACKET` — RX больше не
  «вешается» навсегда после первого OVW.
- `RING_HAS_PACKET` закалён против `FF`-чтений: при невозможных значениях
  `ISR=0xFF` или `BNRY/CURR` вне диапазона `[PSTART,PSTOP)` сообщает «кольцо
  пусто» (вместо каскада в чтение мусора из адреса 0x0000), и вызывающий код
  делает tick (окно закрывается → шина/IRQ восстанавливаются).
- Циклы ожидания (ping/ifup/resolve) сливают кольцо подряд и списывают таймаут
  на каждый кадр — больше нет растягивания таймаута на десятки секунд под
  флудом.
- Откатан повтор чтения тела по `0xFFFF`: он молотил DMA-движок (abort→read×4)
  и коррелировал с бо́льшим числом зависаний `CR=FF`; сбой устойчивый, повтор
  не помогает.

Это «потолок» программных мер: убрать сами ошибки шины из одного `LD A,(HL)`
нельзя.

---

## 7. Предложения по исправлению (в FPGA)

### 7.1. Быстрый эксперимент — увеличить окно ожидания

`SP2_ACEX.TDF`, строка 601:

```
WHEN B"1XXX" => WT_R[] = 4;      -- было ~95 нс
```

Варианты:
- Перезагружать `7` вместо `4` (счётчик 3-битный, максимум) → ~167 нс.
- Расширить счётчик до `WT_R[3..0]` (строка объявления ~232) и грузить `15`
  → ~357 нс (ближе к стандартному ISA-I/O ~375 нс и выше).

Плюсы: одна-две строки, без изменения платы. Минус: замедляет **все**
ROM/ISA-обращения (ROM/cache на той же шине). Для проверки приемлемо.
Критерий успеха — `NICRAM` (см. §8).

### 7.2. Правильное решение — учесть `IOCHRDY`

Цель: пока медленная карта держит `IOCHRDY` (A10) низким во время ISA-доступа,
удерживать Z80 `/WAIT` низким; отпустить, когда карта подняла `IOCHRDY` (данные
готовы). Это штатный механизм ISA, которого в ACEX нет. Есть два пути.

#### 7.2.A. Завести `IOCHRDY` внутрь ACEX

- Подать цепь `I/O CH READY` (A10 всех слотов, с подтяжкой к VCC) на вход ACEX.
- В прошивке: добавить вход, синхронизировать (2 триггера от метастабильности),
  и подмешать терм ожидания только на ISA-доступе:

  ```
  IOCHRDY : INPUT;                                  -- новый вывод
  RDY[1..0] : DFF;  RDY[].clk = CLK42;
  RDY[0] = IOCHRDY;  RDY[1] = RDY[0];               -- синхронизатор
  WAIT_IOCH = CS_ISA & !RDY[1];                     -- ждать, пока не готова, на ISA
  /WAIT_ALL = DECODE./WAIT & WAIT_ROM & WAIT_ORIG & !WAIT_IOCH;  -- добавить терм
  ```

- **Препятствие:** ACEX занят полностью (147/147, см. §5.5а) — свободного вывода
  нет. Нужно либо высвободить существующий пин (перетасовать функции, инвазивно),
  либо вести `IOCHRDY` на вторую ПЛИС (MAX) — но у MAX нет ISA/wait-логики и,
  судя по всему, нет связи с `/WAIT`. Плюс дорожка A10→вывод и перепрошивка
  Quartus. Из-за исчерпания выводов сейчас это **наименее удобный путь**.

#### 7.2.B. Обработать `IOCHRDY` вне ACEX (без перепрошивки)

Возможно потому, что `/WAIT` — открытый сток с обратным чтением (§5.5б): внешний
открытый сток может удерживать `/WAIT` низким без конфликта с ПЛИС. Всё нужное
(IOCHRDY/A10, ISA-стробы, системный такт, линия `/WAIT`) уже на плате/слоте;
схему можно разместить даже на **райзере/переходнике между картой и слотом** —
основную плату не трогаем.

- **B2 (правильный, рекомендуется для «вне ПЛИС»):** малая ПЛИС/GAL
  (ATF16V8 / небольшой CPLD), тактируемая системным клоком. На активном
  ISA-стробе синхронизирует `IOCHRDY`, держит `/WAIT` в 0 (открытый сток), пока
  `IOCHRDY` низкий; отпускает, когда высокий. По таймингу самый корректный —
  ждём ровно столько, сколько просит карта.
  Входы: `IORD1/`, `IOWR1/` (или `MEMR1//MEMW1/`), `IOCHRDY`, `CLK`; выход:
  открытый сток на `/WAIT`.
- **B1 (простой костыль, без `IOCHRDY`):** одновибратор/счётчик (74HC123 или
  малый счётчик), который на каждом ISA-стробе тянет `/WAIT` в 0 на фикс. время
  (например +300…400 нс) и отпускает. `IOCHRDY` не нужен. По сути «внешний
  `WT_R`»: замедляет все ISA-доступы одинаково, но гарантирует время порту данных.

**Условия для B (обязательно):**
- тянуть `/WAIT` только открытым стоком/через диод (не толкать в 1) — нет
  конфликта с ПЛИС;
- убедиться, что на `/WAIT` есть подтяжка к VCC (схема TRI её предполагает —
  проверить на плате);
- асинхронный `IOCHRDY` синхронизировать к системному такту;
- действовать **только во время ISA-доступа** (квалификация стробом), иначе
  застопоришь не-ISA циклы;
- фиксированные ~95 нс ПЛИС всё равно идут — внешний wait добавляется поверх
  (wired-AND на `/WAIT`), итоговое ожидание = max(95 нс, внешнее).

### 7.3. Воспроизведение в MAME

Чтобы итерировать FPGA-фикс не перепрошивая железо: смоделировать в
MAME-машине Спринтера «фиксированный wait без `IOCHRDY`» (короткий ISA-цикл +
подтяжка шины к 0xFF на undriven), чтобы baseline воспроизводил `FF` на порту
данных. Тогда правку wait можно проверять в эмуляторе.

---

## 8. План проверки: NICRAM как регресс-тест DMA

`NICRAM.EXE` — это **прямой тест корректности remote-DMA**: пишет паттерн в
пакетную RAM по `0x4000` через порт данных DMA, читает обратно тем же портом и
сравнивает побайтово на размерах **16 / 64 / 256 / 1536** байт. Сети не
требует.

Использовать как go/no-go:
- До фикса (текущее железо под нагрузкой) — ожидаем падение `GOT=FF`.
- После каждого изменения `WT_R` / заведения `IOCHRDY` — `NICRAM` должен
  устойчиво проходить **все 4 размера**, в т.ч. при повторных прогонах.
- Только после стабильного `NICRAM` имеет смысл проверять `ifup`/`ping`.

Дополнительно для регрессии драйвера: `NICINFO` (чтение PROM/MAC) и
`NICLB` (внутренний loopback).

---

## 9. Ссылки на файлы и строки

| Что | Где |
|-----|-----|
| Схема ISA (страница 8 «ISA-B BUS») | `board/spdx_release_v306_25.05.2025/docs/sp-dx-sch.pdf` |
| Подтяжки шины данных 4K3 (R201–R208) | там же, верхняя часть стр. 8 |
| Дешифратор 74HC138 + гейты стробов | там же, левая часть стр. 8 |
| Слоты ISA: A1=IOCH, A10=I/O CH READY, B8=NOWS/0WS, стробы `*MEMR/*IOR/` | стр. 8 (X36/X37) |
| Прошивка ПЛИС | `sprinter_bios/src/altera/quartus/acex/SP2_ACEX.TDF` |
| Список выводов FPGA (нет `IOCHRDY/NOWS`) | строки 26–72 |
| Фиксированный wait `WT_R` (~95 нс) | строки 593–601 |
| `/WAIT` = открытый сток + обратное чтение | строки 905, 909 |
| `/IORD`,`/IOWR` (порты Z80) | строки 310–311 |
| Устройство и распиновка ACEX (147/147 I/O; `/WAIT`=PIN 18) | `SP2_ACEX.qsf` (`DEVICE EP1K30QC208-3`, `set_location_assignment PIN_*`) |
| Рабочий аналог по той же ISA (UART) | `sprinter_wifi/network/src/lib/esplib.asm` (`UART_READ`) |
| Драйвер RTL8019AS | `src/lib/rtl8019.asm`, `src/include/rtl8019.inc`, `src/lib/isa.asm` |
| Тест DMA | `src/apps/nicram.asm` |
| Внешний контекст | форум nedoPC t=20283 «Проект Sprinternet» (W5100) |
