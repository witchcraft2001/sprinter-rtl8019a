# Независимая проверка isa_issues.md и предложения по исправлению

Документ для разработчиков (source-tree only, не входит в `DIST_*`).
Результат независимой перепроверки выводов [`isa_issues.md`](isa_issues.md)
по первоисточникам: прошивка ACEX (`sprinter_bios`), схема платы v3.06,
даташит RTL8019AS, исходники драйвера. Дата: 2026-07-12.

---

## ⚠ ПОПРАВКА (2026-07-12, вечер): перекрёстная проверка с isa_issues_gpt_research.md

После выхода этого документа независимый анализ
[`isa_issues_gpt_research.md`](isa_issues_gpt_research.md) выявил два факта,
которые я и isa_issues.md пропустили. Оба перепроверены мной по
первоисточникам и **подтверждаются**:

1. **`IOCHRDY` НА v3.06 УЖЕ СОЕДИНЁН С `/WAIT` — через диод VD12.**
   Нетлист `board/.../pcad/sp-dx.net` (файл содержит не-ASCII байты, grep
   нужен с `-a` — поэтому текстовый поиск «не находил» цепь): сеть `IOCHRDY`
   = {A10 всех четырёх слотов X36–X39, VD12 пин 2, R47 пин 1}; R47 = 4K3 →
   VCC; сеть `WAIT/` = {VD12 пин 1, ACEX PIN_18, Z84C15 пин 11, X28:A38,
   R37 (1K → VCC)}. VD12 = LL4148. Мои утверждения «у A10 именованной цепи
   нет / никуда не идёт» (§5.4) и вся посылка «IOCHRDY некуда подать» —
   **ОШИБОЧНЫ**; правильные схемные выводы см. в gpt-документе, §4.
   Следствие: механизм «wait запрошен, но проигнорирован» возможен только в
   форме **маргинального уровня**: низкий уровень на `WAIT/` через кремниевый
   диод ≈ VOL(IOCHRDY) + VF(LL4148) ≈ 1.0–1.2 В при спецификации VIL(max)
   Z84C15 = 0.8 В — вне спецификации, распознавание зависит от порога
   конкретного чипа/температуры/питания. Это по-прежнему кандидат в
   первопричину data-port `FF`, но фикс — **измерить уровень и заменить VD12
   на Шоттки (VF ≈ 0.2–0.3 В)**, а не заводить пин в ACEX. Вариант с ACEX
   (ex-`/HALT`, §8.3) остаётся корректным запасным путём, если диодная
   развязка не пройдёт по уровням; «внешний GAL» (§8.4) как базовый фикс
   снимается.
2. **`RTL_PSTOP_INIT=0x80` нарушает даташит для 8-битного режима.**
   Даташит RTL8019AS, описание PSTOP: «In 8 bit mode the PSTOP register
   should not exceed to 0x60» (проверено дословно). Пин 96 `IOCS16B/SLOT16`
   при включении сэмплируется и в 8-битном слоте подтянут вниз → на
   ISA-8 Спринтера чип гарантированно в 8-битном режиме, доступно только
   8 КБ packet RAM (`0x4000..0x5FFF`). Linux `ne.c` содержит этот же
   workaround (stop_page=0x60 при 8-битном DCR и ID 0x50/0x70). Наш драйвер
   с `PSTOP=0x80` гоняет RX-кольцо по несуществующим страницам `0x60..0x7F`
   — сильное объяснение деградации под трафиком и тел `FFFF`; MAME маскирует
   (моделирует все 16 КБ безусловно). **Первоочередной программный фикс:
   `RTL_PSTOP_INIT=0x60`.**

Также верные претензии gpt-документа к текущему дереву: `RTL_DMA_SETTLE=32`
(≈ 30–35 мс с DI на кадр 1518 байт — вернуть 0), печать при открытом
ISA-окне в NICRAM/NICINFO (нарушение собственной дисциплины проекта),
`DELAY_2MS` при открытом окне в `RESET`/`RECOVER_OVERFLOW`, page-2 регистры
читаемы по даташиту (снапшот-тени прячут фактическое состояние).

Что из настоящего документа **остаётся в силе**: проверка прошивки ACEX
(нет входа IOCHRDY, WT_R ≈ 95–120 нс, open-drain `/WAIT`, `/HALT` не
используется), поправки к isa_issues.md (§3), тайминговая модель §4 (теперь
с оговоркой про VD12), факты §5.1–§5.3 (включая 7 МГц-обход — он остаётся
валидным экспериментом), патчи §8.2/§8.3 как запасные варианты. Раздел §9
(план) следует читать вместе с объединённым планом: **сначала PSTOP=0x60 +
SETTLE=0 + capture-then-print, затем NICRAM-статистика (в т.ч. на 7 МГц),
затем измерение уровня `WAIT/`/VD12 осциллографом, и только потом правки
ACEX.**

Проверялись четыре вопроса:

1. Насколько выводы isa_issues.md реальны, а не «выдуманы»?
2. Можно ли обойти проблему чисто софтварно?
3. Есть ли другие факторы/причины?
4. Что конкретно править (включая конфигурацию ACEX в `sprinter_bios`)?

---

## 1. TL;DR

- **Корневая причина из isa_issues.md подтверждается по всем первоисточникам.**
  ПЛИС ACEX действительно не принимает `IOCHRDY` (входа нет ни в списке
  портов `SP2_ACEX.TDF`, ни в `SP2_ACEX.qsf`), `/WAIT` для ISA/ROM — жёсткий
  счётчик `WT_R` (перезагрузка 4, такт 42 МГц ≈ 95–120 нс), шина данных
  `BD0..BD7` подтянута к VCC через 4.3 КОм (R201–R208 видны на стр. 8 схемы)
  — «недоехавшее» чтение защёлкивается как `0xFF`.
- **Даташит RTL8019AS замыкает механизм количественно** (раздел 7.3 (1)):
  когда wait НЕ нужен, чип выдаёт данные за ≤ 60 нс (T4) — регистры обычно
  успевают; когда wait НУЖЕН, чип опускает `IOCHRDY` в течение ≤ 50 нс ПОСЛЕ
  фронта строба (T3) и выставляет данные только к моменту снятия `IOCHRDY`
  (T5). Масштаб удержания — сотни наносекунд (аналогичный узел BROM: 125–350
  нс). Фиксированное окно Спринтера (~170–240 нс от строба до защёлки при
  21 МГц, см. §4) такие циклы не покрывает. **Важно: `IOCHRDY` действует и на
  запись (IOWB)** — нестабильность записи объясняется тем же механизмом.
- **Найдены неточности isa_issues.md, не меняющие вывод** (§3): `DECODE./WAIT`
  — это НЕ обратное чтение линии `/WAIT` (это выход генератора ожиданий DCP);
  назначенных выводов 148, а не 147 (147 I/O + выделенный клок `TG42`);
  «окно ~95 нс» — это длительность `/WAIT`, реальное окно данных больше
  (~170–240 нс), но всё равно мало.
- **Новые факты, которых в isa_issues.md нет** (§5):
  1. **Вход `/HALT` (PIN_182) объявлен в `SP2_ACEX.TDF`, но не используется
     ни одним выражением прошивки** — готовый кандидат под `IOCHRDY` без
     перетасовки функций (нужна только перекоммутация на плате).
  2. На линии `WAIT/` есть **подтяжка 1 КОм** (страница CPU схемы) — внешний
     open-drain генератор ожидания безопасен и быстр.
  3. **Тактовую частоту Z80 генерирует сама ПЛИС** (`CLK_Z80`, 7/21 МГц,
     бит `TB_SW` переключается программно через порт `#3C`) — отсюда
     **новый чисто софтверный обход**: на время remote-DMA переходить на
     7 МГц, окно данных растёт до ~290–430 нс (§6.2).
- **Ответы на вопросы:** выводы реальные (§3); полный софтверный фикс
  невозможен, но 7 МГц-обход может дать практическую стабильность (§6);
  других правдоподобных первопричин не найдено — вторичные факторы уже
  закрыты драйвером (§7); правки ACEX — §8.

Рекомендуемый порядок действий (§9): **эксперимент №0 — прогнать `NICRAM`
в не-турбо режиме (F12+Ctrl+Shift), ни строчки кода** → если проходит,
теория окончательно доказана и появляется рабочий обход; дальше — драйверный
slow-DMA, затем прошивочные варианты.

---

## 2. Методика

| Источник | Что проверено |
|----------|---------------|
| `sprinter_bios/src/altera/quartus/acex/SP2_ACEX.TDF` | полный список портов (строки 26–76), `WT_R`/`WAIT_ROM` (593–602), `CS_ISA`/`PRE_ISA` (585–610), `/WAIT_ALL`/`TRI` (905–909), `ISA_PORT` (558–560), неиспользуемые входы |
| `sprinter_bios/.../acex/DCP.TDF` | `DECODE./WAIT` = `/IO_WAIT & /MR_WAIT` (537–572), генерация `CLK_Z80` (275), `TURBO`/`TB_SW` (262, 649), таблица I/O-wait `W_TAB` (554–565) |
| `SP2_ACEX.qsf` | устройство `EP1K30QC208-3`, 148 уникальных `PIN_*`, `PIN_18=/WAIT`, `PIN_182=/HALT` |
| `board/spdx_release_v306_25.05.2025/docs/sp-dx-sch.pdf`, стр. 8 | отрисована в 300 dpi и прочитана по фрагментам: DD7 74HC138 (входы/разрешения), DD20 74HC32 (гейтирование `RD/`), R188/R194/R195/R209 51 Ом, R201–R208 4K3 на BD0..7, слоты X36–X38 (A10 = I/O CH READY без именованной цепи, B8 = NOWS→`0WS`, B20 = CLOCK(8.33)←`CLKOUT`), R210–R212 4K3 на IRQ1/DRQ1/DACK1 |
| страница CPU той же схемы | подтяжки 1 КОм на `WAIT/`, `RESET/`, `BUSRQ1/` |
| Даташит RTL8019AS (Realtek, 2001; ethernut.de/pdf/8019asds.pdf) | тайминги ISA I/O R/W (T1–T8), BROM (IOCHRDY low width), примечание про PnP и IOCHRDY |
| `src/lib/isa.asm`, `src/lib/rtl8019.asm` | текущая дисциплина окна, DMA-циклы (IX-адресация, `DMA_SETTLE`) |
| `sprinter_ai_doc/manual` (9.2, 9.3, 1.4) | турбо-механика (TB_SW/TURBO_HAND), порт `#3C/#7C` → DCPP `#C6`, CMOS `#1B` бит 0 |
| Обе копии `SP2_ACEX.TDF` | `diff` — идентичны (quartus/acex и acex/k30) |

---

## 3. Проверка утверждений isa_issues.md

| # | Утверждение isa_issues.md | Вердикт |
|---|---------------------------|---------|
| 1 | Шина BD0..7 подтянута 4.3 КОм к VCC (R201–R208); undriven-чтение = `0xFF` | **Подтверждено** (схема, стр. 8, видны все 8 резисторов 4K3 → VCC) |
| 2 | Тракт стробов v3.06 структурно чистый, «паразитный /MEMR» не причина | **Подтверждено на уровне гейтов** (§3.1) |
| 3 | В ACEX нет входа `IOCHRDY`; `/WAIT` — фикс. счётчик `WT_R`=4 при 42 МГц ≈ 95 нс | **Подтверждено**, с уточнением про реальное окно (§4) |
| 4 | Z80-паузы между чтениями (`RTL_DMA_SETTLE`) не помогают в принципе | **Подтверждено** — проблема внутри цикла, а не между циклами |
| 5 | ACEX занят «147/147», свободных выводов нет | **Почти**: уникальных назначений 148 (147 user I/O + выделенный клок `TG42`). Практический вывод «нет свободного user I/O» верен, **но есть логически неиспользуемый вход `/HALT`** (§5.1) |
| 6 | `/WAIT` — открытый сток; «ПЛИС читает линию обратно» | **Наполовину**: `TRI(/WAIT_ALL,!/WAIT_ALL)` действительно никогда не гонит линию в 1 (open-drain-семантика) → внешний wired-AND безопасен. Но `DECODE./WAIT` — НЕ обратное чтение пина: это выход генератора ожиданий DCP (`/IO_WAIT & /MR_WAIT`, `DCP.TDF:572`). Пин `/WAIT` в логике прошивки нигде не читается. На вывод это не влияет |
| 7 | Регистры успевают, порт данных DMA — нет; под нагрузкой хуже | **Подтверждено и обосновано даташитом** (§4): без wait — T4 ≤ 60 нс; с wait — данные позже окна. Под RX-трафиком локальный DMA чаще занимает FIFO → чаще нужен wait |
| 8 | Порядок фиксов: прошивка WT_R → внешний генератор → IOCHRDY в ACEX | **Скорректирован**: благодаря находке `/HALT` вариант «IOCHRDY в ACEX» становится реалистичнее, а софт-обход через 7 МГц вообще не требует железа (§6.2, §8) |

Итого: **выводы isa_issues.md не выдуманы** — все ключевые факты
воспроизводятся по первоисточникам; найденные неточности (`DECODE./WAIT`,
147→148, framing «95 нс») вторичны и вывода не меняют.

### 3.1. Тракт стробов — подтверждение чистоты (детали)

Схема, стр. 8 (прочитано с кропа 300 dpi):

- DD7 = 74HC138N: адресные входы `A0=RA14`, `A1=WR/`, `A2=RA15`; разрешения
  `E0(G1)=RA17`, `/E1(G2A)=RA16`, `/E2(G2B)=CS_CASH`.
- Чтения: `Q6 → DD20:2 (74HC32, второй вход RD/) → R194 51 Ом → IORD1/`;
  `Q7 → DD20:1 (RD/) → R195 → IORD2/`; `MEMR1//MEMR2/` — аналогично через
  DD20:4/DD20:3 с `RD/`.
- Записи: `Q4 → R188 → IOWR1/`, `Q5 → R209 → IOWR2/` — напрямую с
  дешифратора; корректно, потому что `WR/` сам является адресным входом A1
  (строб записи по построению не длиннее `/WR`).
- Гонки: адресные входы дешифратора в пределах цикла меняет только `WR/`
  (одиночный бит) — однобитовое переключение 74HC138 не даёт иголок на
  третьих выходах, а выходы чтения дополнительно замкнуты `RD/` через OR.
  Ложный `IORD` в цикле записи исключён (во время записи `RD/`=1).

То есть «мусорные /MEMR» с форумной темы Sprinternet на v3.06 действительно
не воспроизводятся структурно — это свойство старых ревизий/другой платы.

Расшифровка байта окна PAGE3 (`0xD0..0xD6`), следует из разводки DD7
(`RA15=бит2` — I/O или память, `RA14=бит1` — слот): `0xD0`=MEM слот 1,
`0xD2`=MEM слот 2, `0xD4`=**I/O слот 1**, `0xD6`=I/O слот 2. Это
подтверждает эмпирический выбор `0xD4` в `isa.asm` (UPDATE 2 из истории
отладки): бит 2, а не бит 1, отличает I/O-режим от memory-режима.

---

## 4. Уточнённая таймовая модель отказа

Числа, которых не хватало в isa_issues.md.

**Сторона Спринтера.** `CLK42` = 42 МГц (23.8 нс/такт). При ISA/ROM-доступе
`WT_R` грузится 4 и считает вниз → `/WAIT` удерживается ≈ 95–120 нс от
начала цикла `/MR`. Окно данных = длительность чтения Z80 от фронта строба
до защёлки (Z80 защёлкивает данные на спаде T3):

| Режим CPU | Такт T | Окно без wait | Вставка от `/WAIT` (95–120 нс) | Итоговое окно |
|-----------|--------|----------------|-------------------------------|----------------|
| Турбо 21 МГц | 47.6 нс | ~95 нс (2T) | +2–3 wait-такта | **~170–240 нс** |
| Нормальный 7 МГц | 143 нс | ~285 нс (2T) | +0–1 wait-такт | **~285–430 нс** |

(Оценка по стандартным диаграммам Z80/Z84C15 без осциллографа; строб
`IORD1/` открывается через ~20–30 нс декодной задержки после `/MR`.)

**Сторона RTL8019AS** (даташит, §7.3 (1), ISA I/O Read/Write):

| Параметр | Значение | Смысл |
|----------|----------|-------|
| T4 | ≤ 50–60 нс | данные готовы от спада `IORB`, **если wait не нужен** |
| T3 | ≤ 50 нс | `IOCHRDY` опускается от спада `IORB`/`IOWB`, **если wait нужен** |
| T5 | ≥ 25 нс | данные валидны до подъёма `IOCHRDY` |
| T6 | 10–30 нс | удержание данных после подъёма `IORB` |
| (BROM T2) | 125–350 нс | ширина удержания `IOCHRDY` на соседнем (BROM) тракте — масштаб внутренних ожиданий чипа |

Механизм в сборе:

1. Регистровые чтения почти всегда попадают в случай «wait не нужен»
   (T4 ≤ 60 нс « 170 нс) → работают.
2. Порт данных remote-DMA требует синхронизации с внутренним 10 МГц ядром
   и арбитража с локальным DMA (приём кадров). Когда FIFO не готов, чип
   говорит об этом ЕДИНСТВЕННЫМ штатным способом — `IOCHRDY` (T3), прося
   продлить цикл на сотни наносекунд. Спринтер не слушает → защёлкивает
   подтяжку `0xFF`.
3. Под RX-нагрузкой локальный DMA занимает FIFO чаще → wait нужен чаще →
   деградация «под трафиком», которая и наблюдалась.
4. **Запись подвержена так же** (T3 явно упоминает `IOWB`): проглоченный
   wait на записи = потерянный/битый байт в пакетной RAM и рассинхрон
   автоинкремента — источник «нестабильной записи» и битых TX-кадров.
5. Изредка wait может понадобиться и на регистровом доступе (синхронизация
   с ядром) — объясняет редкие `CR=FF/ISR=FF` вне DMA-циклов под нагрузкой.
6. В 7 МГц-режиме окно (~285–430 нс) перекрывает большинство wait-случаев
   (масштаб 125–350 нс) → предсказание: **в не-турбо режиме NICRAM должен
   проходить радикально стабильнее** — это главный дешёвый эксперимент.

Модель объясняет ВСЕ наблюдения из isa_issues.md §2 одним механизмом:
интермиттентность (готовность FIFO — гонка), корреляцию с нагрузкой,
`GOT=FF` на порту данных при живых регистрах, эпизоды полного «зависания»
(рассинхрон автоинкремента + битые записи в CR/RSAR), и тот факт, что
раньше «всё работало» (на тихой шине большинство циклов успевает).

---

## 5. Новые факты (дополнение к isa_issues.md)

### 5.1. Вход `/HALT` (PIN_182) прошивкой не используется

`/HALT : INPUT;` объявлен в `SP2_ACEX.TDF:36` и назначен на PIN_182 в qsf,
но **ни одно выражение прошивки его не читает** (проверено grep по всем
TDF проекта ACEX; единственное упоминание — сама декларация). То есть
«свободного user I/O нет» — формально верно, но **логически свободный вход
есть**. Чтобы отдать его под `IOCHRDY`, нужно только:

- на плате: отсоединить PIN_182 от цепи Z80 `HALT/` (перерезать дорожку или
  поднять ногу) и кинуть провод на цепь A10 слотов. Отрезать обязательно:
  `HALT/` — push-pull выход Z80, конфликт с open-collector `IOCHRDY`
  недопустим; Z80 периодически исполняет `HALT` (ожидание прерывания в DSS);
- в прошивке: переименовать порт и добавить 3 строки логики (§8.3);
- подтяжка: у цепи A10 на плате подтяжки не видно (в отличие от
  IRQ1/DRQ1/DACK1 с R210–R212 4K3) — добавить резистор 4.7–10 КОм к +5 В
  на цепь A10 (входы ACEX 1K 5V-tolerant).

Потеря функции: нулевая — сигнал `HALT` в текущей прошивке и так
игнорируется.

### 5.2. На линии `/WAIT` есть подтяжка 1 КОм

Страница CPU схемы: `WAIT/` подтянута к VCC через 1 КОм (кластер R33/R37/
R40/R45 вместе с `RESET/`, `BUSRQ1/`). Значит: (а) TRI-семантика прошивки
(тянуть в 0 / отпускать) корректна и быстра; (б) внешний open-drain
генератор ожидания (§8.4) полностью безопасен — это снимает оговорку
isa_issues.md «проверить подтяжку на плате».

### 5.3. Такт Z80 генерирует ПЛИС; турбо переключается программно

`CLKZ1 = DECODE.CLK_Z80` (`SP2_ACEX.TDF:894`), `DCP.TDF:275`:
`CLK_Z80 = (CLK21 & TURBO) or (7 МГц-делитель & !TURBO)` — мультиплексор
регистровый, переключение на лету штатное (им пользуется горячая клавиша).
`TURBO = TB_SW & TURBO_HAND`, где:

- `TURBO_HAND` — аппаратный триггер горячей клавиши (**F12+Ctrl+Shift**,
  сброс → турбо включён);
- `TB_SW` — **программная защёлка**: порт `#3C`/`#7C` (DCPP-код `#C6`),
  `TB_SW := D0` при D1=1 (`DCP.TDF:649`); стартовое значение — CMOS `#1B`
  бит 0.

Отсюда обход §6.2. Для эксперимента даже порт не нужен — достаточно
горячей клавиши.

### 5.4. Прочее подтверждённое окружение

- Слот получает клок: `CLKOUT → B20 CLOCK(8.33)`; рядом цепь `BCLK14`.
  RTL8019AS его не требует (свой кварц 20 МГц), но внешнему генератору
  ожидания (§8.4) есть от чего тактоваться прямо на разъёме.
- `B8 NOWS` заведён в цепь `0WS` (не используется прошивкой), `A1 I/O CH
  CHECK` → цепь `IOCH`. `A10 I/O CH READY` соединён между слотами и никуда
  не идёт — ровно как писал isa_issues.md.
- Драйверная дисциплина окна (DI/open/работа/close/EI, capture-then-print)
  в `isa.asm`/`rtl8019.asm` соответствует задокументированной в CLAUDE.md —
  вторичные источники нестабильности (прерывание поверх окна, DSS-вызовы
  при открытом окне) действительно закрыты на стороне софта.

---

## 6. Можно ли обойти софтварно?

### 6.1. Полностью — нет (подтверждение вывода isa_issues.md)

Wait должен случиться **внутри** ISA-цикла (между фронтом строба и защёлкой
данных). У Z80 нет инструкций с управляемой длиной строба; `/WAIT` формирует
только ПЛИС. Паузы между обращениями, повторные чтения через переустановку
`RSAR` (тот же тайминг каждого отдельного цикла), любые ретраи — не меняют
вероятность отказа единичного цикла. Потолок «мягких» мер (FF-толерантное
кольцо, восстановление OVW, отказ от промискуитета) уже достигнут в драйвере.

### 6.2. Частично — да: режим 7 МГц на время обмена с чипом (НОВОЕ)

Окно данных в не-турбо режиме — ~285–430 нс (§4), это уровень канонического
ISA-цикла и с запасом больше типичных ожиданий чипа. Три ступени внедрения:

1. **Эксперимент №0 (ноль кода):** на реальном железе выключить турбо
   горячей клавишей (F12+Ctrl+Shift; альтернатива — CMOS `#1B` бит 0 = 0 и
   перезагрузка), прогнать `NICRAM` несколько раз, затем `ifup`/`ping` под
   фоновым трафиком. Если стабильность резко выросла — модель §4 доказана
   экспериментально, и обход существует.
2. **Драйверный slow-ISA (рекомендуемый софт-фикс):** в `ISA.ISA_OPEN`
   (после `DI`) снимать `TB_SW` (`OUT (#3C), %00000010`), в `ISA.ISA_CLOSE`
   (до `EI`) возвращать (`OUT (#3C), %00000011`). Порт `#3C`: D1=1 разрешает
   запись бита турбо D0; D2=0 не трогает CNF; `#3C` (A6=0) не включает
   системный ROM (в отличие от `#7C`). Оговорки: (а) семантику соседних
   защёлок порта надо перепроверить на живой системе до коммита (влияние на
   DCP-`SYS` при Scorpion-битах `SC0` ≠ 0 — в контексте DSS-приложения SC=0
   и влияния нет); (б) если пользователь работает в не-турбо, «возврат» в
   турбо нежелателен — прочитать текущее состояние нельзя, поэтому либо
   калибровка скорости при старте утилиты (CTC-метод из мануала 9.2), либо
   ключ `NET.CFG` (например `RTL_SLOWISA=1/0/auto`). Цена: замедляются
   только участки с открытым окном; DMA-циклы и так ограничены темпом
   ~мкс/байт, реальная потеря пропускной способности умеренная.
3. **Точечный вариант:** переключать частоту только вокруг
   `DMA_READ`/`DMA_WRITE`/`SEND_FRAME` циклов, оставив регистровые доступы
   на 21 МГц — быстрее, но не закрывает редкие регистровые wait-случаи (§4
   п.5). Начать стоит с варианта 2 (проще и полнее), вариант 3 — как
   оптимизация после стабилизации.

Честная оценка: это **обход, а не фикс** — `IOCHRDY` по-прежнему
игнорируется, и теоретический хвост случаев >430 нс остаётся (чип имеет
право держать `IOCHRDY` дольше, спецификация верхнюю границу для remote-DMA
не фиксирует). Но по масштабам из даташита вероятность попадания в хвост
мала; статистику даст `NICRAM` в цикле.

### 6.3. Что ещё стоит сделать в драйвере независимо от обхода

- После каждого remote-DMA сверять `CRDA` (current remote DMA address,
  регистры 0x08/0x09 page 0) с ожидаемым конечным адресом — дешёвый
  детектор рассинхрона автоинкремента (пропущенный/лишний строб), даёт
  диагностике точный признак «шина соврала» вместо косвенных `FF`.
- В `NICRAM` добавить многопроходный режим (например `NICRAM 100`) для
  статистики отказов до/после каждого изменения — критерий «стабильно»
  должен быть числом, а не впечатлением.

---

## 7. Другие факторы: рассмотрены и оценены

| Фактор | Оценка |
|--------|--------|
| Паразитные стробы (форумная «кривизна ISA») | На v3.06 исключено структурно (§3.1). На старых ревизиях — реально, но это не наша плата |
| Гонка дешифратора при записи (WR/ как адресный вход 74HC138) | Проверено: однобитовые переходы 74HC138 без иголок, чтения дополнительно гейтированы `RD/`. Чисто |
| Прерывание поверх открытого окна, DSS-вызовы при открытом окне | Реальные вторичные факторы; уже устранены в драйвере (DI/EI, capture-then-print). Остаточные симптомы (FF-дампы после печати) — артефакты диагностики, не первопричина |
| Режим окна PAGE3 (memory vs I/O) | Закрыто: `0xD4` = I/O слот 1 (§3.1), подтверждено разводкой дешифратора |
| 16-бит режим / DCR.WTS | Исключено: 8-бит путь принудительный (`DCR=0x48`), PROM «doubled» — норма 8-битного чтения |
| AEN | `PORT_ISA=0` → AEN=0 постоянно; для I/O-декода RTL8019AS корректно |
| Отсутствие ISA BCLK у карты | RTL8019AS от шинного клока не зависит (свой кварц); B20 на плате есть |
| Питание/контакт разъёма | Не исключается как усугубляющий фон (эпизод спонтанного выключения был объяснён IRQ-голоданием и ушёл после фикса), но все воспроизводимые симптомы объясняются таймингом §4 без привлечения питания |
| Ревизия чипа / фальшивый RTL8019AS | Маловероятно: чип корректно работает в границах своих таймингов (регистры, RDC, счётчики), отказ строго в wait-случаях |
| MAME не воспроизводит | Ожидаемо: у MAME нет модели внутрицикловых таймингов/IOCHRDY. Расхождение MAME-vs-железо — свойство эмулятора, не улика против модели |

Новых правдоподобных первопричин, конкурирующих с «фиксированное окно +
игнорируемый IOCHRDY», не обнаружено.

---

## 8. Предложения по исправлению

Отсортировано по возрастанию инвазивности. Все правки прошивки — относительно
`sprinter_bios/src/altera/quartus/acex/` (вторая копия `src/altera/acex/k30/`
идентична — патчить синхронно или удалить дубль).

### 8.1. P0. Эксперимент без кода: NICRAM на 7 МГц

См. §6.2 п.1. Стоимость — минуты; результат — go/no-go для всей модели.
Если не-турбо НЕ помогает — модель §4 требует пересмотра, и прошивочные
правки 8.2–8.4 откладываются до выяснения (это тоже ценный результат).

### 8.2. P1. Прошивка, минимальный риск: отдельный удлинённый wait только для ISA

Уточнение §7.1 isa_issues.md: не трогать общий `WT_R` (он обслуживает и
ROM/кэш — глобальное замедление не нужно), а завести **второй счётчик только
для ISA-окна**. `SP2_ACEX.TDF`, зона строк 229–232 (декларации) и 593–602:

```text
VARIABLE
    ...
    WAIT_ISAX   : NODE;         -- добавить
    WT_I[3..0]  : DFF;          -- добавить (4 бита)

BEGIN
    ...
    -- было: WAIT_ROMX = LCELL(CS_ROM & CS_ISA);
    WAIT_ROMX   = LCELL(CS_ROM);            -- ROM: как раньше, 4 такта
    WAIT_ISAX   = LCELL(CS_ISA);            -- ISA: свой счётчик

    WAIT_ROM    = (WAIT_ROMX or DFF((WT_R[] == 0),CLK42,!WAIT_ROMX,))
                & (WAIT_ISAX or DFF((WT_I[] == 0),CLK42,!WAIT_ISAX,));

    -- WT_R[2..0]: без изменений (перезагрузка 4)

    WT_I[].clk  = CLK42;
    CASE (WAIT_ISAX,WT_I[]) IS
        WHEN 0        => WT_I[] = 0;
        WHEN B"00001" => WT_I[] = WT_I[] - 1;
        WHEN B"0001X" => WT_I[] = WT_I[] - 1;
        WHEN B"001XX" => WT_I[] = WT_I[] - 1;
        WHEN B"01XXX" => WT_I[] = WT_I[] - 1;
        WHEN B"1XXXX" => WT_I[] = 15;        -- ~357 нс ISA-wait
    END CASE;
```

Эффект: окно данных ISA при 21 МГц ≈ 95 нс + 357 нс ≈ **450 нс** (уровень
стандартного ISA-цикла), ROM/кэш не затронуты. Цена — ~+260 нс на каждое
ISA-обращение (для сетевого драйвера несущественно) и ~6 LE в ACEX.
Это всё ещё «глухой» фиксированный wait (IOCHRDY игнорируется), но покрытие
wait-случаев чипа по масштабам даташита — практически полное.

### 8.3. P2. Прошивка + перекоммутация: IOCHRDY через бывший пин /HALT

Правильное решение по стандарту ISA, ставшее реалистичным благодаря §5.1.

Плата: PIN_182 отделить от цепи `HALT/`, соединить с цепью A10 слотов,
добавить подтяжку 4.7–10 КОм → +5 В на A10 (у ACEX 1K входы 5V-tolerant).

`SP2_ACEX.qsf`:

```text
set_location_assignment PIN_182 -to IOCHRDY   ;# было -to /HALT
```

`SP2_ACEX.TDF`:

```text
-   /HALT       : INPUT;
+   IOCHRDY     : INPUT;    -- ex-/HALT (PIN_182), теперь A10 ISA-слотов

VARIABLE
    RDY[1..0]   : DFF;      -- синхронизатор
    WAIT_IOCH   : NODE;

BEGIN
    RDY[].clk   = CLK42;
    RDY0        = IOCHRDY;
    RDY1        = RDY0;
    WAIT_IOCH   = LCELL(!CS_ISA & !RDY1);    -- ISA-доступ и карта просит ждать

    -- строка 905, добавить терм:
    /WAIT_ALL   = (DECODE./WAIT & WAIT_ROM & WAIT_ORIG & !WAIT_IOCH);
```

Тайминговая корректность стыка: `IOCHRDY` падает за ≤ 50 нс от строба (T3),
а штатный `WT_R` и так держит `/WAIT` первые ~95–120 нс — к моменту его
истечения синхронизированный `RDY1` уже отражает запрос карты; окно между
термами не образуется. Настоятельно рекомендуемая страховка — сторожевой
счётчик, снимающий `WAIT_IOCH` через ~12 мкс (9-бит DFF на CLK42), чтобы
зависшая/выдернутая карта не вешала Z80 намертво:

```text
    WT_TO[8..0] : DFF;
    WT_TO[].clk = CLK42;
    CASE (WAIT_IOCH,WT_TO[]) IS
        WHEN 0          => WT_TO[] = 0;
        WHEN B"0XXXXXXXX1" => WT_TO[] = WT_TO[] - 1;   -- (паттерны по аналогии с WT_I)
        WHEN B"1XXXXXXXXX" => WT_TO[] = 511;
    END CASE;
    -- терм: !(WAIT_IOCH & (WT_TO[] != 0)) вместо !WAIT_IOCH
```

(Точную AHDL-запись каскада паттернов оставить на этап компиляции; смысл —
как у `WT_R`.) Вариант 8.2 и 8.3 совместимы: 8.2 можно оставить как
минимальный гарантированный wait, 8.3 — как динамическое продление.

### 8.4. P3. Вне ПЛИС: внешний генератор ожидания (если перешивать нельзя)

Вариант B из isa_issues.md §7.2.B — подтверждён и уточнён:

- электрически безопасен: `/WAIT` = open-drain со стороны ACEX (§3 п.6) и
  имеет штатную подтяжку 1 КОм (§5.2);
- всё сырьё есть прямо на слоте/райзере: `IOR/ (B14)`, `IOW/ (B13)`,
  `IOCHRDY (A10)`, `CLOCK 8.33 (B20)`; тянуть провод придётся только к
  цепи `WAIT/` (например, к выводу R33 или PIN_18 ACEX);
- B2 (GAL/ATF16V8: держать `WAIT/` пока строб активен и `IOCHRDY` низкий,
  + таймаут) корректнее; B1 (одновибратор ~400 нс на каждый строб) — быстрый
  костыль, эквивалентный 8.2, но без перепрошивки;
- итоговое ожидание = wired-AND с фиксированными ~95–120 нс ПЛИС.

### 8.5. MAME: что реально смоделировать

Полноценно повторить внутрицикловый тайминг в MAME нельзя (шина в эмуляторе
атомарна). Практичный суррогат для регрессий драйвера — fault-injection в
`rtl8019as.cpp` (устройство своё): опциональный параметр «вероятность
вернуть 0xFF из data-port чтения / проглотить data-port запись», включаемый
в тестовой машине. Это не проверит прошивочный фикс, но позволит гонять
FF-толерантность драйвера (кольцо, OVW-восстановление, CRDA-детектор из
§6.3) без железа. Проверку правок ACEX даст только реальная плата + NICRAM.

---

## 9. Сводный план действий

| Шаг | Действие | Стоимость | Критерий успеха |
|-----|----------|-----------|-----------------|
| 0 | `NICRAM` ×N в не-турбо (F12+Ctrl+Shift), затем `ifup`/`ping` под трафиком | минуты | все размеры стабильно OK ⇒ модель доказана |
| 1 | Драйвер: slow-ISA в `ISA_OPEN/CLOSE` (`TB_SW` через `#3C`), ключ `NET.CFG` | часы | `NICRAM`/`ping` стабильны в турбо-сессии пользователя |
| 2 | Драйвер: CRDA-сверка после DMA + многопроходный `NICRAM` | часы | численная статистика отказов |
| 3 | Прошивка 8.2 (`WT_I`=15 только для ISA) | Quartus-сборка + перешивка | `NICRAM` стабилен на 21 МГц без slow-ISA |
| 4 | Прошивка 8.3 (IOCHRDY на ex-/HALT) + подтяжка A10 | перекоммутация + перешивка | канонический ISA; slow-ISA и `WT_I` можно ослабить |
| 5 | (альтернатива 3–4) внешний GAL/райзер 8.4 | пайка | то же |

Шаги 0–2 не требуют доступа к прошивке и дают либо рабочую сеть, либо
опровержение модели — начинать с них.

---

## 10. Ссылки

| Что | Где |
|-----|-----|
| Список портов ACEX (нет IOCHRDY; `/HALT` не используется) | `sprinter_bios/src/altera/quartus/acex/SP2_ACEX.TDF:26-76` |
| `WT_R`/`WAIT_ROM` (фикс. ~95–120 нс) | там же, строки 593–602 |
| `PRE_ISA`/`CS_ISA` (декод окна `0xD0..0xD6`) | там же, строки 587, 604 |
| `/WAIT_ALL`, `TRI` (open-drain, без чтения пина) | там же, строки 905, 909 |
| `DECODE./WAIT` = `/IO_WAIT & /MR_WAIT` | `.../acex/DCP.TDF:537-572` |
| `CLK_Z80`, `TURBO`, `TB_SW` (порт `#C6` = `#3C/#7C`) | `.../acex/DCP.TDF:262-276, 641-660` |
| Пины: 148 назначений; `/WAIT`=PIN_18, `/HALT`=PIN_182 | `.../acex/SP2_ACEX.qsf` |
| Схема ISA: 74HC138/74HC32, 51 Ом, R201–R208 4K3, слоты | `board/spdx_release_v306_25.05.2025/docs/sp-dx-sch.pdf` стр. 8 |
| Подтяжка 1 КОм на `WAIT/` | та же схема, страница CPU (кластер R33/R37/R40/R45) |
| Тайминги RTL8019AS T1–T8, BROM IOCHRDY | даташит RTL8019AS 2001-04-02, §7.3 (ethernut.de/pdf/8019asds.pdf) |
| Турбо-механика, CMOS `#1B`, порт `#3C/#7C` | `sprinter_ai_doc/manual/09_advanced/02_turbo.md`, `03_hidden_features.md`, `01_architecture/04_port_map.md` |
| Дисциплина ISA-окна в драйвере | `src/lib/isa.asm`, `src/lib/rtl8019.asm` (`DMA_READ/DMA_WRITE`), CLAUDE.md «ISA window discipline» |
| Исходный анализ | [`isa_issues.md`](isa_issues.md) |
